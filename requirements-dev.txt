-r requirements.txt
pytest==9.1.1
openpyxl==3.1.5
anthropic==1.8.0  # eval runner only: the Claude transport; the app runs on Gemini
mcp==2.2.0  # the MCP server (mcp_server.py); the app does not import it
